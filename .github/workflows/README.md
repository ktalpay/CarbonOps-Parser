# GitHub Workflows

CI workflows are intentionally not configured yet.

Workflows should be added after implementation and test structure exist for the Python and .NET paths. Until then, the repository uses documentation and review checklists instead of workflow YAML files.

Do not add placeholder workflow YAML files without a task that defines the checks they should run.
